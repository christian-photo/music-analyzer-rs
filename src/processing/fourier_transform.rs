pub struct FrequencyData {
    pub frequency: f64,
    pub amplitude: f64,
    pub phase: f64,
}

pub fn discrete_fourier_transform(signal: &[f64]) -> Vec<FrequencyData> {
    let signal: Vec<FrequencyData> = Vec::new();
    todo!("Implement discrete fourier transform");
    return signal;
}
